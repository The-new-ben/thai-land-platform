<?php
/**
 * Thailand Platform uninstall boundary.
 *
 * Version 0.1.0 stores no persistent data, so uninstall is intentionally a
 * no-op. Future versions must enumerate and test every deliberate deletion.
 *
 * @package Thailand_Platform
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
